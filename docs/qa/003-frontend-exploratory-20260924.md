# D4 前端综合探索测试（2026-09-24）

基线：`bc06097`；网页控制台 `http://192.168.5.194:5173/`，D4 ControlHost `:18443`，Hardware 运行组 epoch 35 / `Armed`。以独立浏览器会话检查 1440×900、768×1024、390×844 三种视口及浅/深色。原始逐步截图、录像和完整复现步骤在本机忽略目录 `.validation/frontend-qa-20260924/report.md`，不随 Git 分发。

## 已执行

- 登录、退出、受保护路由重定向与登录后回跳；仪表盘、四个显示页、媒体源、背景音乐、预案、设置全部分组与移动底栏/“更多”入口。主题切换刷新后仍保留。
- 2 号窗口用临时图片走浏览器“上传并打开”闭环：`POST /api/sources/upload/` 201、`POST /api/playback/2/open/` 200，ControlHost 状态 `playing`；关闭 200 后删除临时源及媒体文件。
- 媒体源网页录入 201、重命名 200、移动 200、删除 200；文件夹创建 201。背景音乐低音量 MP3 上传 201、加入列表/播放/暂停/停止/清空均返回 200。预案创建 201、过长名称校验、预览、编辑 200、删除 200；未激活预案。
- 设置页的 SSE 手动重连产生新的 `/api/events/` 200；源列表单请求模拟断线时有错误提示，撤销模拟后刷新回到 200。未发现未处理的浏览器 JavaScript 异常。
- 自动门禁：`pnpm test` 40/40、`pnpm run typecheck` 通过、`pnpm run build:web` 成功。构建提示主 JS chunk 约 1.07 MB，尚未测量其对真实设备加载的影响。

## 可复现发现

| ID | 优先级 | 现象 |
| --- | --- | --- |
| ISSUE-001 | 低 | 空白登录表单只显示两行笼统“登录失败”，无字段级必填提示。 |
| ISSUE-002 | 中 | 空白播放主机地址提交泄漏英文 `Invalid URL`，还将控制链路误标为不可达。 |
| ISSUE-003 | 低 | 1440px 仪表盘“大屏模式”说明把“窗口”拆开，出现单字孤行。 |
| ISSUE-004 | 低 | 空白文件夹名称点击“创建”后对话框静默关闭，没有错误提示或新文件夹。 |
| ISSUE-005 | 高 | 文件夹卡片内“编辑”按钮点击后进入文件夹，无法编辑/删除；按钮嵌套语义也不正确。 |
| ISSUE-006 | 中 | 媒体库选中某文件夹后，背景音乐页继承 `folder_id` 却无切换控件；在该页上传的根目录音频被隐藏，需回媒体库切“全部源”。 |
| ISSUE-007 | 中 | 背景音乐“暂停”请求返回 200，但等待并刷新后仍显示“播放中”及“暂停”按钮；本轮未判定物理声音是否暂停。 |
| ISSUE-008 | 中 | 账户页三个密码输入均无关联标签，辅助技术只读到相同的“请输入”。 |
| ISSUE-009 | 中 | axe-core 4.12.1 报设置卡片 4 个 `role=heading` 节点缺少 `aria-level`。 |
| ISSUE-010 | 低 | 浅色侧栏普通导航文字对比度约 4.15:1，低于 WCAG AA 的 4.5:1；设置活动标签约 4.33:1。 |
| ISSUE-011 | 中 | 768px 平板媒体库仍采用宽表格，操作列被藏在横向滚动区；滚到右侧后名称不可见、窄列逐字换行。390px 手机版卡片布局正常。 |
| ISSUE-012 | 低 | 设置→开发仍称根目录 `.env` 为当前后端配置；D4 根目录无该文件，实际使用 `frontend/.env` 与 DataRoot 日志。 |

计数：高 1、中 6、低 5，共 12 项。每项截图/录像、重复步骤及网络证据见上述本机报告；它们是本轮 QA 发现，**未作为代码已修复项**。

## 安全边界与收尾

未触发设备电源、视频墙模式、系统音量、显示器落位、预案激活、修改密码、重置全部窗口及物理四屏冒烟；这些操作会改变现场状态，不能从前端浏览测试请求推定授权。D4 上本轮临时图片、网页、音频、预案和文件夹均已删除；原有媒体源 ID 1/2 保留，四窗口 `idle`，源/文件夹/预案/播放列表分别为 2/0/0/0。ControlHost 健康检查 200，七个受管进程仍在，`control-host.err.log` 为 0 字节；浏览器与 API 测试会话已注销。
